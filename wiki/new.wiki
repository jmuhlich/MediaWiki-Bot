#summary Documentation of the new() function

The new() method allows you to create a new perlwikipedia object. It is used like this:
{{{
my $user='myusername';
my $object=Perlwikipedia->new($user);
}}}
