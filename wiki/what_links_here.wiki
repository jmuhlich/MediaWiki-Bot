==what_links_here($pagename)==

Returns an array containing a list of all pages linking to $pagename as anonymous hashes. The hash structure is 'title' and 'type', the type being a 'transclusion', 'redirect', or neither.